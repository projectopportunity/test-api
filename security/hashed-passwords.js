/* eslint-env mocha */

describe('the user account passwords that are stored in the database', () => {
  before(() => {
    // create user
    // get database record
  })

  after(() => {
    // delete user
  })

  it('should be hashed', () => {
    // hash known password
    // compare to database value
    // should match
  })
})
